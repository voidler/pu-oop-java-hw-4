import java.awt.*;

public class GPS extends  BaseModel{
    public GPS(Color color) {
        super(color);
    }
    public GPS(Color color , int mode) {
        super(color, mode);
    }
}